# Runtime dependencies: what the application actually imports.
#
# This file was previously aspirational — it installed pandas, polars, numpy,
# scikit-learn, web3, passlib and others that nothing imported, most of which
# were not even installed in working environments. Anything a later phase needs
# is added when the code that needs it lands.
#
# Everything here is local and open-source. No paid service, no managed
# database, no hosted API.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Neo4j driver. Imported lazily inside Neo4jGraphRepository, so the application
# still starts and serves every non-graph route when it is absent.
neo4j>=5.28.0

# What makes `.env` work: pydantic-settings reads env files through it.
python-dotenv>=1.0.1
